# hibernate-view-safe-fiz
The example demonstrates an approach to fix LazyInitializationException and other similar case, which is based of subclassing EntityManager and closing after Controller-View
